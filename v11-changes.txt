Arrival Screen and Travel screen Seed selection widget. Give the seed section a text block "Which seed do you wish to carry with you into the [location name]?"

The seed selection should use the larger seed buttons with the seed icon and the seed name, like the Nursery has.

Even though Vision Rules dont say that the locations need to be discovered, only include discovered locations when you check how many locations match a given rule on a given pilgrim.

The traveling screen has a progress bar. We should make this progress bar look like a path (curvy dotted line, brown, from left to right with an X on each end. The progress along the line is shown with a meeple, made of a circle and a triangle underneath, and an arrow showing the direction. When starting travelling, the meeple pilgrim moves left to right, and the arrow points right. When reversing direction, the meeple moves left and the arrow points left. 

On the Arrival Screen, always show the core seed icon for the location you are arriving at.

Update Vision after changing the seed in a pot.

On the map, You can select any visited location or location that neighbours visited locations and it will calculate the fastest time to travel there. The queued up moves are sotred on the pilgrim, and the next queued travel is automatically started. The Arrival screen lists any locations you pass through since you loaded the app, and any queues locations. The seed choices gets updated on these auto travels as well.

The Travelling screen replaces the Location screen in the Location Tab. While travelling, the Map and Record tabs should be accessable.

Selecting a location in the map screen that you can travel to should show the time it will take in the Travel To Button.

Seed symbols and icons should be a minimum of 64px across the entire app.

Add a widget to the bottom of the Map Screen, which shows the  lastSeen pot data and the core seed for the selected location (if it is visited).