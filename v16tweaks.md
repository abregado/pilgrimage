remove the background color and outline from pot circles. 
Move the seed icon to 6 oclock (bottom)on the pot circles

Remove the arrival screen all together, just arrive automatically.

On desktop, the three columns should each take 33% of the horizontal space.

Clicking anywhere on the map that isnt a location should clear the selection.

The border on a completed vision rule should not pulse.

Energy should be visible at the top of the Location and Travel screens, and not visible in the Vision screen.

The Location title should not be visible on the Vision column. Instead use the heading Vision.

Vision rules should show the seed icons of the plants they require. 

Vision rules that require adjacency to empty spaces should be removed.

Fast travel becomes Teleport (lets call it Dendriport). Just move the pilgrim there instantly, without triggering the travel screen.

Plant icons on the map can be twice as large.

On desktop, if the aspect ratio is 8:9 or >8:9 (less than 8 over 9) then put the Vision column into the tabbed nav on the right, and run a two column layout.

 On desktop, Add a border on the right side of the tabbed navigation column.
 
 When a tree is older than DEAD_TICKS, dont remove it, just use the dead version of its graphics and display it as "Dead". Never clear pots automatically.
 
 Use an svg iamge for the pilgrim and flip it when it is going left. Make a placeholder for me to replace.
 